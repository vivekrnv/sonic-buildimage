
SPATH       := $($(MLX_TRIO_DRIVER)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/mlx-trio.mk rules/mlx-trio.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(MLX_TRIO_DRIVER)_CACHE_MODE  := GIT_CONTENT_SHA
$(MLX_TRIO_DRIVER)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(MLX_TRIO_DRIVER)_DEP_FILES   := $(DEP_FILES)