
SPATH       := $($(TMFIFO_DRIVER)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/tmfifo.mk rules/tmfifo.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(TMFIFO_DRIVER)_CACHE_MODE  := GIT_CONTENT_SHA
$(TMFIFO_DRIVER)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(TMFIFO_DRIVER)_DEP_FILES   := $(DEP_FILES)
