
SPATH       := $($(BLUEFIELD_EDAC)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/bluefield-edac.mk rules/bluefield-edac.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(BLUEFIELD_EDAC)_CACHE_MODE  := GIT_CONTENT_SHA
$(BLUEFIELD_EDAC)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(BLUEFIELD_EDAC)_DEP_FILES   := $(DEP_FILES)