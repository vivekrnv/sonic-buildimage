
DPATH := $($(DOCKER_SYNCD_BLUEFIELD_RPC)_PATH)
DEP_FILES := $(SONIC_COMMON_FILES_LIST) $(PLATFORM_PATH)/$(RECIPE_DIR)/docker-syncd-bluefield-rpc.mk $(PLATFORM_PATH)/$(RECIPE_DIR)/docker-syncd-bluefield-rpc.dep
DEP_FILES += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES += $(shell git ls-files -- $(DPATH))

$(DOCKER_SYNCD_BLUEFIELD_RPC)_CACHE_MODE := GIT_CONTENT_SHA
$(DOCKER_SYNCD_BLUEFIELD_RPC)_DEP_FLAGS := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_SYNCD_BLUEFIELD_RPC)_DEP_FILES := $(DEP_FILES)
